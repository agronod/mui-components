import { Meta, Story, Canvas } from "@storybook/addon-docs";
import {
  Grid,
  Button,
  Checkbox,
  Link,
  RadioGroup,
  Radio,
  FormControlLabel,
  FormGroup,
  Stack,
  Box,
  MenuItem
} from "@mui/material";
import { Select } from "./";
import { BrowserRouter } from "react-router-dom";

<Meta
  title="Components/Selects"
  parameters={{
    viewMode: "docs",
  }}
/>

# Selects

<Canvas>
  <Story name="Selects" args={{}}>
    <BrowserRouter>
      <Stack ml={3} spacing={2} direction="column">
        <Box>
          <Stack spacing={2} direction="row">
            <Select
              value={10}
              id="outlined-basic"
              variant="outlined"
            >
              <MenuItem value={10}>Select me</MenuItem>
              <MenuItem value={10}>Or select this</MenuItem>
            </Select>            
            <Select id="filled-basic" label="Filled" variant="filled" >
              <MenuItem value={10}>Select me</MenuItem>
              <MenuItem value={10}>Or select this</MenuItem>
            </Select>
            <Select
              id="standard-basic"
              label="Standard"
              variant="standard"
            >
              <MenuItem value={10}>Select me</MenuItem>
              <MenuItem value={10}>Or select this</MenuItem>
            </Select>
          </Stack>
        </Box>
        <Box>
          <Stack spacing={5} direction="row">
            <Select
              id="outlined-error"
              
              variant="outlined"
              error
              helperText="Incorrect entry."
            >
              <MenuItem value={10}>Select me</MenuItem>
              <MenuItem value={10}>Or select this</MenuItem>
            </Select>
            <Select
              id="filled-error"
              label="Filled"
              variant="filled"
              error
              helperText="Incorrect entry."
            >
              <MenuItem value={10}>Select me</MenuItem>
              <MenuItem value={10}>Or select this</MenuItem>
            </Select>            
            <Select
              id="standard-error"
              label="Standard"
              variant="standard"
              error
              helperText="Incorrect entry."
            >
              <MenuItem value={10}>Select me</MenuItem>
              <MenuItem value={10}>Or select this</MenuItem>
            </Select>            
          </Stack>
        </Box>
      </Stack>
    </BrowserRouter>
  </Story>
</Canvas>
