import { Meta, Story, Canvas } from "@storybook/addon-docs";
import {
  Grid,
  Button,
  Checkbox,
  Link,
  RadioGroup,
  Radio,
  FormControlLabel,
  FormGroup,
  Stack,
  Box,
  MenuItem,
  Typography
} from "@mui/material";
import { Tooltip } from "./";
import { TextField } from "../TextField";

import { BrowserRouter } from "react-router-dom";

<Meta
  title="Components/Tooltip"
  parameters={{
    viewMode: "docs",
  }}
/>

# Tooltip

<Canvas>
  <Story name="Selects" args={{}}>
    <BrowserRouter>
      <Stack ml={3} spacing={2} direction="column">
        <Box>
          <Stack spacing={2} direction="row">
            <Tooltip
              title={<Typography>Title text</Typography>}
              placement="top-start"
            >
              <TextField
                id="outlined-basic"
                label="Outlined"
                variant="outlined"
              />
            </Tooltip>
          </Stack>
        </Box>     
      </Stack>
    </BrowserRouter>
  </Story>
</Canvas>
